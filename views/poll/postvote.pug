extends ../layout

block content
    h2 Results: #{poll.text}
    .results-container
        .results
        .poll-options
            for opt in poll.options
                .poll-option #{opt.text} - #{opt.votes}

block scripts
    script(src='/js/results.js')
